
통합구현

1. JSON 개요
JSON(JavaScript Object Notation) - 자바스크립트 객체 표기법방식
-예)카카오 각종 API(카카오 개발자 센터)

왜 사용할까? 개발자가 모두 할수는 없어 서비스를 사용하는 업체와 연동하기 위해서..





2. Jackson 의존 설정 - 응답, 요청

일반적인 스프링을 사용할 경우 아래 두가지를 mvn에서 찾아 사용하면 된다.
  - jackson databind
  - Jackson Datatype : JSR310 : Data and Time API
		    ( java.time 패키지 )
  참고 ) JSR - Java Specification Requests

* 스프링 부트는 기본 셋팅이 완료되어 있다.





3. @RestController
  - JSON 응답과 요청 처리 Controller
  - 자바 객체 반환하면 -> getter를 호출해서 JSON 형식으로 생성
     -> 응답 객체에는 반드시 getter가 있어야 한다!

  - 반환값 없으면 - 응답 헤더 content-type:text/plain
  




4. 날짜 형식 변환 처리
  - @ JsonFormat

4-2. JSON 변환 처리
  - @Jsonlgnore : 특정항목 제외




5. @ResponseEntiy
응답 + body(출력)


  status(응답 코드)
  body(T t) - 응답시 출력 객체
  build() - 응답 출력 데이터 x





6. @ResponseBody
  - 일반 컨트롤러(@Controller)에서 특정 요청 메서드만 Rest로 사용하는 경우

7. RequestBody
- 커맨드 객체의 매핑은 기본적으로 POST일때는 Content-Type : application/x-www-form-urlencoded
  ->어떤방식인가? 키=값(value)
- appliction/json : JSON 형식이면 서버에서 자동 변환x
- @RequestBody 커맨드 객체 : 변환 o.

  - jackson databind - ObjectMapper : JSON 문자열 -> 자바 객체 변환

  - 스프링 : RestTemplate


ARC- Advanced Rest Client


8. @ExceptionHandler
- @RestControllerAdvice : Rest 컨트롤러 공통기능



























  